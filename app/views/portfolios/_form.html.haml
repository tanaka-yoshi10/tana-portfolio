= simple_form_for(@portfolio) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :area, as: :radio_buttons
    = f.input :category, as: :radio_buttons
    = f.input :ratio

  .form-actions
    = f.button :submit, class: 'btn'
